# Hooks Notes

Hooks are not enabled yet.

Good future candidates:
- run a backend lint or syntax check after Node.js file edits
- block dangerous shell commands during Claude-driven sessions
- run a smoke-validation checklist when auth or RBAC files change

Only add real hooks after confirming the exact hook format supported by your Claude Code setup in Together IDE.
